import React from 'react';
import { Header } from "./components";

const app = () => {
  return (
    <div className="w-screen h-screen flex flex-col">
      <Header />
    </div>
  );
};

export default app